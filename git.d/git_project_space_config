# This is a sample for git configuration that would sit in a project folder and
# would be loaded via envrc exported variable
# This config wuld then further load the ~/.gitconfig

[user]
	email = spam.this.unlucky.dude@gmail.com
	name = Nobody In Particular

[include]
    path = ~/.gitconfig

#[url "git@github.com:/some/project/"]
#    insteadOf = https://github.com/some/project

